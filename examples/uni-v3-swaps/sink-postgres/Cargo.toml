[package]
name = "uni-v3-sink-postgres"
version = "0.1.0"
edition = "2021"

# Build with: cargo build --target wasm32-wasip2 --release
[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }

[package.metadata.component]
package = "liminal:sink-postgres"
